package blocks

import "github.com/onee-only/miner-node/blockchain/transactions"

func MineTxs(txs *transactions.TxS) *Block {
	block := &Block{
		// Difficulty: ,  // should add this thing on mempool
	}

	for {

	}
}
